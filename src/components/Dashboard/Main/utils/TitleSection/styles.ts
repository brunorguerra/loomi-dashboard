import styled from "styled-components";

export const Title = styled.div`
    padding: 4rem 4rem 3.2rem;
    h2 {
        color: var(--purple);
        font-size: 2.4rem;
        font-weight: bold;
    }
`;
