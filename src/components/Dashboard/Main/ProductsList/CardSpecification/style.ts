import styled from "styled-components";

export const Container = styled.div`
    padding: 0.6rem 1.6rem;
    border-radius: 1.8rem;
    background-color: var(--titleOpacity);
    p {
        font-size: 1.6rem;
    }
`;
